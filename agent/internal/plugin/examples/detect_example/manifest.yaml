apiVersion: v1
name: detect-example
version: 0.1.0
entry: ./detect-example.so
artifactDigest: FILL_ME_WITH_SHA256
tasks:
  - name: detect-example
    kind: respond
    capabilities: ["detect", "risk"]
metadata:
  sandbox: required
resources:
  cpu: 300m
  memory: 256Mi
signature:
  algorithm: ed25519
  publicKey: UPDATE_ME
  value: UPDATE_ME
